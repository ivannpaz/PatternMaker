/**
 * Handles the frame creation
 *
 * @return {object}
 */
var DrawingFrame = (function() {

    /**
     * Class Constructor.
     *
     * @param {array}
     */
    function DrawingFrame(param) {
        this.param = param;
    }

    return DrawingFrame;

})();
